{
  "name": "jira-playbook",
  "version": "1.0.0",
  "description": "A command line interface to fetch JIRA issues using the JIRA REST API",
  "main": "playbook.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alan Torrance",
  "license": "Apache 2.0",
  "dependencies": {
    "axios": "^0.21.0",
    "chalk": "^4.0.0",
    "inquirer": "^7.0.0"
  }
}
